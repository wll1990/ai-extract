[Client uploads file]
        |
        v
SkillMaterialController.upload()              [HTTP POST /admin/materials/upload]
        |
        v
MaterialCleaningService.uploadMaterialToSpace()
        |
        v
MaterialCleaningService.uploadMaterial()       [create Space+Skill+Material, save file, DB INSERT]
        |  status="uploaded"
        v
--- SCHEDULER picks up (every 30s) ---
        |
MaterialCleaningScheduler.tryParseOne()        [lock, call parseFile, unlock]
        |
        v
MaterialCleaningService.parseFile()            [HTTP -> AI parse service -> raw text -> parsedContent]
        |  status="uploaded" (parsedContent now populated)
        v
--- SCHEDULER picks up (every 30s) ---
        |
MaterialCleaningScheduler.tryCleanOne()        [lock]
        |
        v
MaterialCleaningService.clean()                [NO @Transactional -- AI calls outside TX]
        |
        |-- Layer 0:  tagContext(rawText)              [AI x1, 3000 char limit]
        |       -> ContextTags
        |
        |-- Layer 1:  noiseCleaner.cleanFormatNoise()  [regex, type-specific rules]
        |-- Layer 2:  businessFilter.filterBusinessNoise()  [rule, line-by-line junk removal]
        |-- Layer 3:  normalizer.normalize()            [rule, normalization + typo fix]
        |       status="cleaning", then "analyzing"
        |
        |-- Layer 4a: semanticChunk(normalized)         [sentence-boundary, 800/1500 chars]
        |       -> List<TextChunk>
        |-- Layer 4b: deduplicate(chunks, spaceId)      [Jaccard n-gram vs existing grains]
        |       -> List<TextChunk> (unique)
        |
        |-- Layer 5a: extractInsights(uniqueChunks)     [AI per chunk, parallel batch of 5]
        |       -> List<ExtractedInsight>
        |-- Layer 5b: classifyScenesBatch(insights)     [AI x1, tags all insights]
        |       -> List<GrainCandidate>
        |
        |-- Layer 6:  verifyGrains(candidates)          [AI, batches of 10, 5-dim scoring]
        |       -> List<GrainCandidate> (approved)
        |
        |-- Layer 7:  mergeOverlapping(verified)        [AI, per duplicate tag group, if >10]
        |       -> List<GrainCandidate> (merged)
        |
        |-- Layer 8:  discoverPatterns(verified)        [AI x1, if >=8 grains]
        |       -> PatternSummary
        |
        |-- Layer 9:  extractFaq(normalized)            [AI x1, 4000 char limit]
        |       -> FAQ JSON string
        |
        |-- Layer 10: generateNarrativeWithLinks()      [AI x1]
        |       -> Narrative JSON string
        |
        |-- Layer 11: generateProfile()                 [AI x1, updates SkillProfile]
        |       -> void (DB side effect)
        |
        |-- finalizeMaterialCleaning()                  [status="analyzed", save metadata]
        v
[MaterialCleaningScheduler continues...]
        |
        |-- saveGrains()                                [DB INSERT ExperienceGrain per grain]
        |-- embeddingService.embed()                    [DashScope text-embedding-v3, 1024-dim]
        |-- markMaterialExtracted()                     [status="extracted"]
        |-- update skill status                         ["generating" -> "reviewing"]
        |-- reportGenerationService.generateAsync()     [async report PDF]
        |-- releaseLock + clearTrace


