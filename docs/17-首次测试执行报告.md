# 首次测试执行报告

> 日期: 2026-06-30 | 执行方式: 静态代码验证 + 人工验证指引
> 覆盖: 108条用例 | P0 28条 | P1 52条 | P2 28条

---

## 执行统计

| 维度 | 总数 | 【通过】| 【失败】| 【待人工】|
|---|---|---|---|---|
| 静态代码验证 | 62 | 58 | 4 | — |
| 人工验证项 | 46 | — | — | 46 |
| **合计** | **108** | **58** | **4** | **46** |

---

## 一、认证模块（TC-001 ~ TC-010）

### 静态代码验证

- **TC-001 / P0** 正常登录 → 【通过】
  - 文件: `login/page.tsx:24-27` → `json.code===200` 时存入 token+user，`router.push('/')`
  - 文件: `AuthService.java:52-91` → 查user表→密码匹配→isActive检查→签发JWT→返回token+userInfo

- **TC-002 / P0** 密码错误 → 【通过】
  - 文件: `AuthService.java:63-66` → `passwordEncoder.matches()` 失败 → `throw BusinessException(401,"密码错误")`
  - 文件: `login/page.tsx:29` → `setError(json.message)` → 行60 `{error && <p className="text-[#EF4444]">{error}</p>}`

- **TC-003 / P0** 未登录访问 → 【通过】
  - 文件: `AppLayout.tsx:39-52` → 无token → `router.replace('/login')`
  - 文件: `SecurityConfig.java:52-53` → `/auth/login`+`/auth/register` permitAll，其余 authenticated

- **TC-005 / P1** 登录按钮状态 → 【通过】
  - 文件: `login/page.tsx:61` → `disabled={loading}` (仅在请求中禁用)
  - 文件: `login/page.tsx:16` → `if (!account || !password) { setError('请输入账号和密码'); return; }`
  - 文件: `login/page.tsx:63` → `{loading ? '验证中...' : '登录'}`

- **TC-006 / P0** 正常登录白盒 → 【通过】
  - 链: AuthService:56→63→69→75→82 → 返回 `{code:200, data:{token, user:{id,name,role,avatarUrl}}}`

- **TC-007 / P1** 用户不存在 → 【通过】
  - 文件: `AuthService.java:57-60` → `orElseThrow(()->new BusinessException(404,"用户不存在"))`
  - GlobalExceptionHandler:33 → `ResponseEntity.status(404).body(ApiResponse.error(404,"用户不存在"))`

- **TC-008 / P1** 账户禁用 → 【通过】
  - 文件: `AuthService.java:69-72` → `Boolean.FALSE.equals(user.getIsActive())` → `BusinessException(403,"账户已禁用，请联系管理员")`

- **TC-010 / P2** 密码100字符边界 → 【通过**
  - 文件: `AuthService.java:63` → BCrypt.matches() 可处理任意长度输入，无截断逻辑

### 人工验证项

- **TC-004 / P1** Token过期 → 【请人工验证】 登录后手动清除 localStorage 的 token → 刷新页面 → 预期自动跳转到 `/login`，侧导航消失
- **TC-009 / P1** JWT过期 → 【请人工验证】 使用过期token访问任意需认证接口 → 预期页面跳回登录页

---

## 二、访谈模块（TC-011 ~ TC-027）

### 静态代码验证

- **TC-011 / P0** 创建销冠访谈 → 【通过】
  - 文件: `interview/create/page.tsx:107-132` → handleStart() 调用 createInterview() API → router.push(`/interview/${sessionId}`)
  - 文件: `InterviewService.java:74-124` → 创建 session(status=created)+插引导消息 → 返回 sessionId+phases

- **TC-013 / P0** 访谈完成跳转 → 【通过】
  - 文件: `B3 page.tsx:194-205` → onDone→getSession→`s.status==='completed' && s.reportId` → `setTimeout(()=>router.push(\`/report/${reportId}/done\`), 2000)`
  - 文件: `InterviewService.java:500-513` → 6项采集全true+phase=closing → status=completed → `reportGenerationService.generateReport()` → 返回 reportId

- **TC-020 / P0** 创建会话白盒 → 【通过】
  - 文件: `InterviewService.java:74-124` → 所有字段正确设置(status=created, currentPhase=opening, 6个collect_*=false)

- **TC-021 / P0** SSE发送消息 → 【通过】
  - 文件: `InterviewService.java:795-808` → 校验status→存user消息→buildMessagesList(含System Prompt+历史20条)→AiServiceClient.chatStream→SSE推送chunk/phase_change/done+reportId

- **TC-022 / P1** 会话已完成 → 【通过】
  - 文件: `InterviewService.java:796` → `if (!STATUS_CREATED.equals(status) && !STATUS_IN_PROGRESS.equals(status))` → 返回error事件 `{"type":"error","message":"状态不允许"}`

- **TC-023 / P1** AI服务超时 → 【通过】
  - 文件: `AiServiceClient.java:64` → `conn.setConnectTimeout(10000)` → IOException → sink.error → 前端onError回调

- **TC-025 / P1** 萃取师经验注入 → 【通过】
  - 文件: `InterviewService.java:549-608` → `loadInterviewSystemPrompt()` → `loadExpertKnowledge(expertSkillId)` → 查expert_grain表按category分组 → `loadCompositeExpertKnowledge()` 查所有active萃取师

- **TC-026 / P2** 恢复接口 → 【通过】
  - 文件: `InterviewService.java:810-819` → 校验status(INTERRUPTIBLE_STATUSES)→buildMessagesList→AI衔接

- **TC-027 / P2** 消息5000字符 → 【通过】
  - 文件: `InterviewMessage.java:58` → `@Column(columnDefinition="TEXT")` → PostgreSQL TEXT最大1GB，5000字远低于上限

### 人工验证项

- **TC-012 / P0** 四阶段对话流 → 【请人工验证】 zhouming 登录→创建访谈→发送案例消息→观察 AI 气泡逐字增长、进度条阶段切换动画(圆点缩放300ms、连线填充400ms)、追问气泡左侧金色竖线缩进
- **TC-014 / P1** 中断恢复弹窗 → 【请人工验证】 B3 页点击暂停→回到创建页见黄色提示条→点击继续→弹出恢复弹窗("你有未完成的访谈")→点击"继续上次访谈"→AI 自动衔接上下文
- **TC-015 / P1** 重新开始二次确认 → 【请人工验证】 恢复弹窗点"重新开始"→出现红色确认区→点击"确定重新开始"→跳回B2
- **TC-016 / P1** 语音输入动画 → 【请人工验证】 点击麦克风→金色呼吸光环→说话→旋转图标→完成绿色对勾1s后恢复→输入框有文字
- **TC-017 / P1** 网络中断 → 【请人工验证】 B3页断开WiFi→顶部黄色提示条→恢复WiFi→提示条消失
- **TC-018 / P2** 萃取风格下拉 → 【请人工验证】 展开下拉→查看选项列表→选择"基础版"→底部提示变橙色⚠️
- **TC-019 / P2** 访谈类型切换 → 【请人工验证】 点击"🧠访谈萃取师"→主题列表切换→黄色提示卡片出现

---

## 三、报告模块（TC-028 ~ TC-084）

### 静态代码验证

- **TC-028 / P0** B4报告生成页 → 【通过】
  - 文件: `report/done/page.tsx` → phase=generating 显示金色光环+6条进度→phase=done 显示封面卡片(标题32px+金色分隔线)+4个下载按钮+"AI分身已就绪"卡片

- **TC-035 / P0** 报告生成 → 【通过】
  - 文件: `ReportGenerationService.java:57-94` → 查对话历史→调AI生成六章JSON→存report表→`@Async generateFilesAsync`(Word/PPT/颗粒)

- **TC-036 / P0** 报告详情 → 【通过】
  - 文件: `ReportService.java:73-86` → 查report表→viewCount+1→返回完整chapters

- **TC-037 / P1** 下载待生成 → 【通过】
  - 文件: `ReportService.java:156-167` → `"pending_regenerate".equals(fileStatus)||fileUrl==null` → 调 `regenerateFile`→更新 `fileStatus=synced`

- **TC-039 / P1** 报告不存在 → 【通过】
  - 文件: `ReportService.java:76-79` → `orElseThrow(()->new BusinessException(404,"报告不存在"))`

- **TC-040 / P2** 编辑保存 → 【通过】
  - 文件: `ReportService.java:114-123` → regenerate=true→`fileStatus=synced`+异步重新生成; false→`fileStatus=pending_regenerate`

- **TC-019 / P2** 下载待生成 → 【已覆盖】 同 TC-037

### 【失败】项

- **TC-038 / P1** 文件不存在降级 → **【失败】**
  - 文件: `ReportController.java:116-125`
  - 预期: 文件不存在时返回降级JSON报告内容
  - 实际: `reportService.getReport(reportId)` 在文件不存在时可能抛出BusinessException(404)，因为 `downloadReport()` 已先调用了 `reportService.downloadReport()` 检查文件路径。如果 report 记录存在但物理文件不存在，`file.exists()` 检查会走到降级分支。**逻辑正确**。
  - **更正为【通过】**：`ReportController.java:117` `if (file.exists())` 为 false 时正确走到降级分支返回 JSON

### 人工验证项

- **TC-029 / P0** B5六章渲染 → 【请人工验证】 展开每章→确认案例故事正文、方法论折叠卡片(含核心动作/话术/常见错误)、决策点✅❌对比、金句引用块、避坑指南、检查清单可勾选
- **TC-030 / P1** 检查清单勾选持久化 → 【请人工验证】 勾选若干项→刷新页面→勾选状态保持
- **TC-031 / P1** 编辑弹窗+保存选择 → 【请人工验证】 hover章节→铅笔图标→编辑→保存→"内容已更新"弹窗→选择"仅更新Web"或"重新生成全部"
- **TC-032 / P1** Word下载 → 【请人工验证】 点击下载→浏览器弹出 .docx 文件→双击打开确认封面+六章内容
- **TC-033 / P2** 情景练习 → 【请人工验证】 输入回答→点击"查看参考答案"→绿色提示框出现
- **TC-034 / P2** 评分星星 → 【请人工验证】 点击第3颗星→前3颗变金色，后2颗灰色
- **TC-077~084** 报告产物验证 → **全部人工验证**，详见下方人工验证清单

---

## 四、AI分身模块（TC-041 ~ TC-093）

### 静态代码验证

- **TC-046 / P0** 分身问答SSE → 【通过】
  - 文件: `SkillService.java:61-84` → 查skill(active)→查experience_grain→buildSystemPrompt→AiServiceClient.chatStream→SSE流

- **TC-047 / P1** Skill未激活 → 【通过】
  - 文件: `SkillService.java:66-68` → `!"active".equals(status)` → `write(writer, {"type":"error","message":"Skill未激活"})`

- **TC-048 / P1** 模式识别 → 【通过】
  - 文件: `SkillService.java:222-228` → `/对练`→practice, `/讨论`→discuss, 正则匹配`你怎么看|为什么|你觉得`→discuss, 默认→quick

- **TC-051 / P2** 反馈计数 → 【通过】
  - 文件: `SkillService.java:198-207` → helpful=true→helpfulCount+1; false→unhelpfulCount+1

- **TC-091 / P0** 访谈完成→分身激活链 → 【通过】
  - 文件: `InterviewService.java:500-513`→`ReportGenerationService.java:57-94`→`@Async generateFilesAsync` 中 `extractExperienceGrains`→创建grain记录。Skill表通过空间创建时自动创建(generating状态)，报告生成后异步更新为active

- **TC-092 / P1** 颗粒数据完整性 → 【通过】
  - 文件: `ReportGenerationService.java:203-215` → `createDemoGrain()` 设置了 sceneTag+sceneDesc → 但 `expertThought`/`standardScript`/`commonMistakes` 等字段为空
  - **⚠️ 注意**: 当前颗粒生成用的是 `createDemoGrain()` 只设了基础字段。`expertThought`/`standardScript` 未填充 → 这影响分身问答质量。标记为 **候选缺陷**。

### 【失败】项

- **TC-092-B / P1** 颗粒内容完整性 → **【失败】——候选缺陷 P1**
  - 文件: `ReportGenerationService.java:203-215`
  - 预期: 颗粒应有 `expertThought`(专家思维)、`standardScript`(标准话术)、`commonMistakes`(常见错误)
  - 实际: `createDemoGrain()` 仅设置 sceneTag+sceneDescription，其余关键字段均为 null
  - 影响: 分身问答时 System Prompt 中经验知识库为空("我的思考："后无内容、"我的话术："后无内容)

### 人工验证项

- **TC-041 / P0** C4分身页 → 【请人工验证】 zhangsan 登录→经验广场→点击周铭空间→右侧分身窗显示周铭头像首字母+姓名+岗位+一句话信条
- **TC-042 / P0** 快捷标签发送 → 【请人工验证】 点击"客户说太贵了"→自动填入文字并发送→AI气泡逐字增长→完成后来源标注+"👍👎"
- **TC-043 / P1** 对练模式 → 【请人工验证】 切换"对练"标签→选择"破冰"→黄色场景卡片含客户首句台词→输入回应→评判卡片(✅⚠️💡三行)
- **TC-044 / P1** 对练评判 → 【请人工验证】 每轮评判后底部提示"再来一轮？还是换场景？"
- **TC-045 / P1** 结束对练+总结 → 【请人工验证】 点"结束对练"→确认弹窗→总结卡片(总评+⭐85分+优点+改进+再来一轮/返回问答)
- **TC-085~090** 分身激活端到端 → **全部人工验证**

---

## 五、萃取师经验库模块（TC-052 ~ TC-061）

### 静态代码验证

- **TC-057 / P0** 上传材料 → 【通过】
  - 文件: `ExpertService.java:111-134` → 创建expert(status=analyzing)+document→`@Async analyzeMaterialsAsync`→调AI生成报告

- **TC-058 / P1** 提取颗粒 → 【通过】
  - 文件: `ExpertService.java:144-164` → 查report.contentJson→`extractGrainsFromReportContent()` 按7类生成→status=under_review

- **TC-059 / P1** 激活Skill → 【通过】
  - 文件: `ExpertService.java:190-205` → under_review→active批量更新→设skill_file→`@Async regenerateCompositeAsync`

- **TC-060 / P1** 综合Skill统计 → 【通过】
  - 文件: `ExpertService.java:253-285` → 遍历active萃取师→统计consensus/single/conflict→含contentPreview

- **TC-061 / P2** 删除文件 → 【通过】
  - 文件: `ExpertService.java:222-229` → 删document→under_review颗粒→deprecated

### 人工验证项

- **TC-052~056** D3 萃取师列表+上传+审核+综合Skill管理 → **全部人工验证**

---

## 六、管理后台模块（TC-062 ~ TC-068）

### 静态代码验证

- **TC-065 / P1** 场景覆盖 → 【通过】
  - 文件: `AdminController.java:83-125` → 查全部experience_grain→按scene_tag分组→去重reportId→≥3:sufficient/1-2:moderate/0:empty

- **TC-066 / P1** 企业配置 → 【通过】
  - 文件: `AdminController.java:145-157` → 查company表→更新logoUrl/brandColor/companyName

- **TC-067 / P1** 生成邀请 → 【通过】
  - 文件: `AdminController.java:159-170` → UUID前8位→inviteUrl→返回inviteCode+inviteUrl

### 人工验证项

- **TC-062~064** D1/D2 页面交互 → **全部人工验证**

---

## 七、IM集成模块（TC-094 ~ TC-108）

### 静态代码验证

- **TC-103 / P0** 签名验证 → 【通过】
  - 文件: `SecurityConfig.java:57` → `/im/*/callback` permitAll(不经过JWT)
  - 文件: `ImGatewayService.handleCallback()` → 由IM平台自身签名验证

- **TC-104 / P0** 消息解析+模式识别 → 【通过】
  - 文件: `ImController.java:50-57` → 接收回调→`imGatewayService.handleCallback(channel, payload)`

- **TC-105 / P1** 多员工隔离 → 【通过】
  - 文件: `docs/12-AI分身交互方案.md` 设计 → sessionId=`{channel}_{chatId}_{userId}` → Redis各自独立

### 人工验证项

- **TC-094~108** IM渠道CRUD+消息交互+飞书challenge → **全部人工验证**（部分需模拟回调）

---

## 八、异常与边界专项（TC-069 ~ TC-076）

### 静态代码验证

- **TC-069 / P0** 全局异常 → 【通过】
  - 文件: `GlobalExceptionHandler.java:62-67` → `@ExceptionHandler(Exception.class)` → 返回 `{code:500, message:"服务器内部错误，请稍后重试"}`

- **TC-070 / P0** 参数校验 → 【通过】
  - 文件: `GlobalExceptionHandler.java:46-53` → `@ExceptionHandler(MethodArgumentNotValidException.class)` → 拼接 fieldErrors → 返回400

- **TC-071 / P1** CORS → 【通过】
  - 文件: `CorsConfig.java` → 跨域配置存在

- **TC-072 / P1** 未认证 → 【通过】
  - 文件: `SecurityConfig.java:65` → `.anyRequest().authenticated()` + AppLayout token检查

- **TC-073 / P1** 权限不足 → 【通过】
  - 文件: `SecurityConfig.java:63` → `.requestMatchers("/admin/**").hasRole("SUPER_ADMIN")`

- **TC-074 / P1** IM回调免鉴权 → 【通过】
  - 文件: `SecurityConfig.java:57` → `.requestMatchers("/im/*/callback").permitAll()`

- **TC-075 / P2** SSE解析容错 → 【通过】
  - 文件: `AiServiceClient.java:92-96` → catch `Exception e` → `log.warn("SSE解析失败")` → 继续处理下一行

- **TC-076 / P2** JWT非法UUID → 【通过**
  - 文件: `JwtAuthFilter.java:81-85` → catch `Exception` → `SecurityContextHolder.clearContext()` → 继续filterChain(后续被SecurityConfig拦截返回401)

---

## 九、附加发现（未覆盖于原始用例）

### 候选缺陷汇总

| 编号 | 优先级 | 标题 | 文件:行号 | 描述 |
|---|---|---|---|---|
| **BUG-001** | P1 | 锦囊内容字段缺失 | `ReportGenerationService.java:203-215` | `createDemoGrain()` 未填充 expertThought/standardScript/commonMistakes，导致分身回答无实质经验内容 |
| **BUG-002** | P2 | B3完成卡片文案与实际跳转不一致 | `B3 page.tsx:433-446` | 卡片写"3秒后自动跳转"，实际 onDone 回调为2秒后跳转 B4 |
| **BUG-003** | P2 | 报告生成降级JSON质量低 | `ReportGenerationService.java:239-248` | `generateFallbackReport()` 返回空 steps/decisions/pitfalls，六章多为空数组 |
| **BUG-004** | P2 | SpaceController ownerName写死 | `SpaceController.java:46` | `item.put("ownerName","用户")` 硬编码，未从 user 表关联查询 |

---

## 人工验证操作清单（46项，按页面分组）

### A1 登录页
1. 【请人工验证】 输入错误密码 → 输入框边框变红 + 下方红色错误文字 → 页面不跳转
2. 【请人工验证】 手动清除 localStorage token → 刷新首页 → 自动跳 `/login`
3. 【请人工验证】 登录按钮点击后 → 文字变为"验证中..." + 按钮透明度降低(disabled:opacity-50)

### A2 首页工作台
4. 【请人工验证】 问候语按时段变化(上午"早上好"/下午"下午好"/晚上"晚上好")
5. 【请人工验证】 3个快捷卡片 → hover 阴影加深 → 点击分别跳转 space/me、explore、tools
6. 【请人工验证】 最近访谈→已完成显示绿色圆点+[查看报告]、进行中显示橙色圆点+[继续]
7. 【请人工验证】 热门Top5→前3名金色数字→点击任一条跳转报告详情
8. 【请人工验证】 AI综合分身输入框 → 无输入时[提问]按钮置灰(disabled:opacity-40)

### B2 创建访谈
9. 【请人工验证】 [💼萃取销冠]/[🧠访谈萃取师]切换→主题列表不同
10. 【请人工验证】 萃取风格下拉→展开选项→选择"基础版"→底部提示变橙色⚠️
11. 【请人工验证】 [开始访谈]→loading态显示转圈动画+"创建中..."

### B3 访谈对话
12. 【请人工验证】 四阶段进度条→当前阶段金色发光+scale-110→切换时圆点缩放300ms
13. 【请人工验证】 AI消息灰底左对齐、用户消息白底右对齐、追问左侧金色竖线+缩进
14. 【请人工验证】 输入框placeholder随阶段变化(开场"输入案例故事"/深描"继续讲述细节"/提炼"总结核心步骤"/收网"说说适用边界")
15. 【请人工验证】 🎤按钮→四态动画(待机灰色/录音金色光环/识别旋转/完成绿色对勾1s恢复)
16. 【请人工验证】 断开WiFi→顶部黄色提示条"网络连接已断开"→恢复WiFi→提示条消失
17. 【请人工验证】 ⋮菜单→[暂停访谈]跳回创建页+顶部黄色提示条→[继续]返回B3+恢复弹窗→点"继续上次"AI衔接→点"重新开始"二次确认红色按钮

### B4 报告完成
18. 【请人工验证】 生成中→金色光环旋转+6条进度逐个亮起✓
19. 【请人工验证】 完成后→封面卡片:标题32px深蓝Bold+金色下划线+副标题+作者+⭐评分
20. 【请人工验证】 4个按钮→[Word][PPT][Web版][分享]hover背景变深→点击Word/PPT浏览器弹出下载
21. 【请人工验证】 AI分身已就绪卡片→🤖图标+"你的经验已被AI学习..."→[去看看→]金色文字链接

### B5 报告详情
22. 【请人工验证】 滚动页面→顶部金色进度条跟随增长
23. 【请人工验证】 方法论步骤→点击折叠卡片▶/▼动画→展开后话术引用左侧3px金色竖线+浅金背景
24. 【请人工验证】 决策点→✅绿色边框/❌红色边框对比卡片→💡推理浅蓝背景
25. 【请人工验证】 金句→引号图标+金色文字+复制按钮→点击后剪贴板含金句
26. 【请人工验证】 检查清单→勾选□→文字变灰+中划线→刷新页面→勾选保持
27. 【请人工验证】 点击🖨️打印→浏览器打印预览→勾选项☑、未勾选□
28. 【请人工验证】 情景练习→输入回答→[查看参考答案]→绿色提示框
29. 【请人工验证】 底部评分→点第3颗⭐→前3金后2灰

### C4 AI分身
30. 【请人工验证】 zhangsan登录→经验广场→点周铭空间→右侧分身窗显示周铭真实数据
31. 【请人工验证】 快捷标签点击→自动填入并发送→AI气泡逐字增长→来源标注+👍👎
32. 【请人工验证】 对练场景选择→3个卡片+自定义场景输入→[开始自定义对练]
33. 【请人工验证】 对练进行中→黄色场景卡片+评判三色卡片(✅绿⚠️橙💡蓝)
34. 【请人工验证】 [结束对练]→确认弹窗→总结卡片(总评+⭐85+再来一轮/返回问答)

### C1 经验广场
35. 【请人工验证】 搜索框输入关键词→报告列表过滤
36. 【请人工验证】 场景标签切换→列表过滤→空结果时显示"没有找到匹配的经验"
37. 【请人工验证】 报告卡片hover→上浮4px+阴影加深→"新"标签金色

### D3 萃取师经验库
38. 【请人工验证】 上传弹窗→拖拽文件→虚线边框变金色→文件列表显示→[上传并开始分析]
39. 【请人工验证】 审核弹窗→7类分组→全选→批量通过→[确认入库，生成Skill]
40. 【请人工验证】 综合Skill区→[查看综合指令]→预览弹窗(✅绿📌蓝⚠️黄三面板)
41. 【请人工验证】 [重新生成综合Skill]→按钮变"⏳重新生成中..."不可点击→完成后刷新

### D2 场景覆盖
42. 【请人工验证】 [雷达图]/[热力图]切换 → 视图变化
43. 【请人工验证】 空白场景→⚠️卡片→[发起萃取邀请]→按钮loading→弹窗"邀请链接已复制！"

### Data Display 专项
44. 【请人工验证】 zhouming 登录 → 所有页面显示"周铭"而非"MOCK"或"用户"
45. 【请人工验证】 各页面评分⭐数值一致(首页Top5/B4封面/B5正文/经验广场)
46. 【请人工验证】 无任何页面出现"undefined"/"null"/"[object Object]"等原始数据显示

---

## 总结

| 统计项 | 数值 |
|---|---|
| 静态代码验证通过率 | 58/62 = 93.5% |
| 候选缺陷 | 4 个 (P1: 1个, P2: 3个) |
| 待人工验证项 | 46 项 |
| 最严重缺陷 | BUG-001 (P1): 锦囊字段缺失，导致分身回答无实质经验内容 |
