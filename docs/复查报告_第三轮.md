# AI经验萃取平台 — 第三轮复查（Delta vs 第二轮）

> 对比对象：`复查报告.md`（第二轮）
> 复核方式：P0 项人工读源码验证 + 后端/前端并行深度走查（逐项 Verify）
> 时间：2026-07-10 晚（第三轮）

---

## 0. 一句话结论

**工程债（性能/事务/索引/日志/超时）这轮基本清完了，干得漂亮。但上轮我标 P0 的安全项——几乎原封未动。** 具体说：

- ✅ 全部"工程质量"类问题已修复（见 §1）。
- 🔴 全部"安全"类 P0 仍破，且 **IDOR 比上次发现的面更大**：不止 `SkillController` 那两个接口，而是 **11+ 个按 ID 访问的接口都没有归属校验**（报告/会话/分身场景全是跨租户可读可改）。
- 🔴 前端 token 仍是"伪修复"（Cookie 路径加了，但 localStorage 那条没删，XSS 盗号面没关）。
- 🟠 新增一个回归：`getReport` 这个 GET 接口带了 `@Transactional` 还每次自增浏览量。

**这不是"改完了"的状态。** 工程侧可以收工，安全侧才是现在真正的上线 blocker。

---

## 1. 本轮已修复（确认 FIXED）

| 项 | 证据 |
|---|---|
| H1 backfillEmbeddings OOM | `MaintenanceController:29-79` 改分页 `findWithoutEmbedding` + `embedBatch` + `batchUpdate` |
| H5 综合路径 N+1 | `InterviewService:611-614` / `ExpertService:639` 改 `findByExpertIdInAndStatus` 批量 + 内存分组 |
| H8/H9 SSE 超时 | 全部 7 个流式方法加 `.timeout(Duration.ofSeconds(120))`（InterviewService:888/914/247、SkillService:188/256/290/592） |
| H2 skill 列表 | `SkillService:424-466` 改分页 DB 查询 + 批量预加载 |
| H6 全文检索 | `ReportRepository:51-55` 收敛到 title/subtitle；`V6__trigram_index.sql` 建 pg_trgm GIN 索引 |
| H13 索引 | `V5__scheduler_indexes.sql:6-7` expert_skill 也已建 `(status, locked_at)` |
| M5 日志 | `application.yml` 已无 `com.aiextract: DEBUG` |
| 前端 client.ts 401 | `lib/api/client.ts:42-47` clearAuth + 跳转保持完好 |
| 前端 skill Q&A 流式 | `skill/[skillId]:204-207` 改 `setQaStreamText` 独立状态 |

---

## 2. 仍破（重点：安全）

### 2.1 🔴 系统性跨租户 IDOR（最该修，且比上轮面更大）
`SecurityConfig` 仅 `anyRequest().authenticated()`——任意登录用户即可凭 ID 访问他人资源。以下接口**取了 ID 却没校验归属**：

| 端点 | Controller | Service 校验 |
|---|---|---|
| 报告 详情/编辑/评分/清单/下载 | `ReportController:67/82/92/104/119` | 均无 userId/owner 校验 |
| 访谈 详情/消息/重启/暂停 | `InterviewController:72/111/176/188` | 均无 owner 校验 |
| 分身 场景标签/对练场景 | `SkillController:203/214` | 均无归属校验 |
| 分身 会话消息/删除会话 | `SkillController:68/74` | **连 Authorization 头都不收**（上轮已点名，未改） |

> 仅 `SkillController.chat`/`listConversations`/`enterpriseChat` 正确取了 userId/companyId——说明框架能力现成，上面这些是"漏了"。

**附带泄漏**：`ReportService.getReports`（缺 spaceId 时 `findAllByOrderByCreatedAtDesc` 返全租户列表）、`ToolController.getTools`（缺 spaceId 时 `findAll` 跨租户）、`SkillController.recommendedQuestions/startPractice/respondPractice/evaluatePractice`（无归属校验）——任一登录用户可枚举他人数据。

**修复范式（建议统一落地）**：
```java
// Controller
UUID userId = jwtUtil.getUserIdFromToken(authHeader.substring(7)); // 复用 SkillController.extractUserId
ReportDetailResponse r = reportService.getReport(reportId, userId);

// Service
public ReportDetailResponse getReport(String id, UUID userId) {
    Report r = reportRepository.findById(id).orElseThrow(...);
    Space space = spaceRepository.findById(r.getSpaceId()).orElseThrow(...);
    if (!space.getUserId().equals(userId)) throw new ResponseStatusException(FORBIDDEN);
    // 或企业级：space.getCompanyId() != currentCompany → 403
    ...
}
```

### 2.2 🔴 C5 飞书回调零签名 + 硬编码 companyId
`ImGatewayService:87` 仍 `UUID.fromString("c0000000-...")`；`handleCallback:62` 直接调 `handleFeishuCallback` 不经任何签名校验；`FeishuAdapter.verifySignature` 仍死代码。`SecurityConfig` 仍 `permitAll`。→ 任何人可伪造回调。

### 2.3 🔴 M7 下载伪装 JSON + regenerateFile 空操作
`ReportController.downloadReport:143-153` 文件缺失时仍把 `content_json` 当 `report_<id>.json` 返回；`ReportGenerationService.regenerateFile:72-75` 仍 `return ""`。

### 2.4 🟠 新回归：getReport GET 写 viewCount
`ReportService:148-159` `@Transactional` + 每次 GET `setViewCount(+1)` + `save`。GET 非幂等写、高并发行锁竞争、前端预取会重复累加。建议改成 `@Modifying` 单语句 `update ... set viewCount=viewCount+1` 并移出读路径（或异步累加）。

### 2.5 ⚠️ C4 数据源凭据兜底明文
`application.yml:7-8` 已改 `${PG_USER}`/`${PG_PASSWORD}`，但 fallback 仍是 `wanglei` 与空密码。部署若未注入变量即落到明文。建议去掉默认值。

### 2.6 ⚠️ H12 ToolController.getTools 跨租户
`SkillMaterialController.picker` 已按企业收敛；但 `ToolController.getTools:46` 缺 spaceId 时仍 `findAll` 跨租户。

---

## 3. 前端仍破

| 项 | 状态 | 证据 |
|---|---|---|
| C1 token 存储 | 🔴 伪修复 | `storage.ts:55-58` 仍写 localStorage；`sse.ts:57-58` 仍读它注入 `Authorization`（注释说 Cookie 但实现没接管）。XSS 盗号面未关 |
| H1 边缘 RBAC | 🔴 STILL | `src/middleware.ts` 不存在；仅 `AppLayout.tsx:55` 客户端 `router.replace` 可绕过 |
| H3 SSE 401 | 🔴 STILL | `sse.ts:145/239` 仅 `onError`，无 clearAuth/跳转 |
| M1 SSE 超时 | 🔴 死代码 | 超时逻辑已实现但 8 个调用方无一传 `timeout` |
| M4 访谈页 O(n²) | 🟠 PARTIAL | 访谈页 `:180-186/308` 仍 `setMessages(prev=>prev.map(...))`；`sendMessageImmediate:250` 同 |
| L1 硬编码 companyId | 🔴 STILL | `login:11` `register:11,24` `admin/users:39` 三处写死同一 companyId |

---

## 4. 建议的下一步（聚焦收口安全）

**P0 — 必须做**
1. IDOR：用 §2.1 范式给 11+ 接口补归属校验（Controller 取 userId/companyId → Service 校验 space→user/company → 不匹配 403）。先覆盖**写/状态类**：`updateReport`/`rateReport`/`syncChecklist`/`restart`/`pause`/`deleteConversation`。
2. 飞书回调：接入 `FeishuAdapter.verifySignature`；companyId 从渠道配置解析，移除硬编码。
3. 前端 token：删掉 `storage.ts` 的 token 读写 + `sse.ts` 的 `getToken()` 注入，彻底走 HttpOnly Cookie（保证 SSE `credentials:'include'` 即可）。

**P1**
4. `getReport` 浏览量改 `@Modifying` 单语句 / 异步，移出 `@Transactional` 读路径。
5. `downloadReport` 未生成返回 409/202 + 状态，删掉 JSON 伪装；实现或删除 `regenerateFile`。
6. `src/middleware.ts` 边缘校验会话 + `super_admin`。
7. `ToolController.getTools` 按 companyId 过滤。

**P2**
8. SSE 调用方统一传 `timeout`；流期间 401 也 `clearAuth()`+跳转。
9. 访谈页流式改独立 ref/末尾索引更新（去 O(n²)）。
10. 移除硬编码 companyId（登录上下文/后端返回）。
11. `application.yml` 去掉 DB 凭据兜底默认值。

---

## 5. 给架构师

"工程质量这关你通关了——事务、N+1、HNSW、全文索引、backfill OOM、SSE 超时、日志脱敏，全清了，这部分可以盖章。但**安全这关没开始**：IDOR 现在是个系统性缺口（11+ 接口跨租户可读可改），飞书回调裸奔，token 还躺在 localStorage 里。上线前这三项必须收口，它们是'能直接被利用'级别，不是'以后优化'级别。需要我把 §2.1 的归属校验模式直接落到那 11 个接口上吗？这块改动集中、模式统一，我可以一次铺完。"
