# AI经验萃取平台 — 设计与实现问题分析报告

> 审查范围：前端（Next.js）、后端（Spring Boot）、AI 服务（Python FastAPI）、部署（docker-compose / application.yml / CLAUDE.md）
> 审查方式：三端并行深度代码走查 + 关键高危项人工复核（已标注「已验证」）
> 标准基准：项目自带 `CLAUDE.md` 工程红线 + 通用资深工程判断

---

## 0. 一句话结论

这个项目的**文档/架构设计与其实际实现已经严重漂移**，而且实现层面还系统性违反了它自己 `CLAUDE.md` 里写下的红线。最刺眼的三件事：

1. **AI 服务名存实亡**——它本应是 LLM/流式引擎，但现在只剩文件解析，`/chat`、`/report/generate` 全是 `raise NotImplementedError`，`/health` 自己承认是 `file-parser-only`。后端已经改用 Spring AI `ChatClient` 直接调 LLM，却把这些调用放在了**HTTP 请求线程里同步 `blockLast()`**——这恰恰是 `CLAUDE.md` 明文禁止的。
2. **安全有多处硬伤**——JWT 密钥硬编码进仓库、前端把 token 存 `localStorage` 又用 `dangerouslySetInnerHTML` 渲染 AI 内容（一键盗号链）、IM 回调零签名校验、报告/会话/对话按 ID 直查无归属校验（IDOR）。
3. **性能红线被踩满**——事务里跑 AI/HTTP、N+1、全表 `findAll()`、全文 `LIKE` 全表扫、`ORDER BY` 表达式让 HNSW 索引失效。

下面按严重程度展开。**已验证** = 我亲自读了源码确认；其余为代码走查结论。

---

## 1. 【架构级 · 已验证】设计说一套，实现做一套

| 设计承诺（README / docker-compose / CLAUDE.md） | 实际代码 |
|---|---|
| AI 服务是 LLM 引擎，提供 `/internal/chat` 流式访谈、`/internal/report/generate` 报告生成 | `routers/interview.py:37-52` 两个端点均 `raise NotImplementedError("DEPRECATED: use Spring AI ChatClient instead")`；`/health` 返回 `{"status":"deprecated","service":"file-parser-only"}` |
| 前端/后端通过 `AI_SERVICE_URL` + `depends_on` 依赖 AI 服务做流式推理 | `ai-service` 仅 `/parse-file` 存活（读文件转文本）。真正推理在 **Java 后端**（`InterviewService.processMessageToWriter` 用 Spring AI `ChatClient` 同步 `blockLast()`，无超时） |
| 「长耗时 AI 调用必须异步化，禁止阻塞 HTTP 线程」（`CLAUDE.md` 红线） | LLM 流式在 Tomcat 请求线程内同步 `blockLast()`（`InterviewController` / `SkillController` 的 chat/respondPractice/enterpriseChat/evaluatePractice），且 `processMessageToWriter:883` / `resumeSessionToWriter:909` **没有超时** |

**问题本质**：架构发生过一次静默重构（Python → Spring AI），但**没清理死代码、没更新文档、没修复分层**。结果就是：docker-compose 还在编排一个"只会读文件"的 Python 服务并把它当核心依赖；后端 LLM 调用落到了最不该落的 HTTP 线程层。

**建议**：
- 要么彻底删除 `ai-service` 这个僵尸服务（连同 `AI_SERVICE_URL` / `depends_on` / 端口 8000），要么真正接回它（见第 4 节）。现在这种半死不活状态最危险。
- 把 Spring AI 的流式调用从 Controller/Service 请求线程迁到 `@Async` + `SseEmitter`/`DeferredResult`，释放 Tomcat 线程，所有 `blockLast`/`block` 必须带 `Duration` 超时，并对客户端断开做 `doOnCancel` 取消订阅。

---

## 2. 【CRITICAL · 安全 · 已验证】密钥与凭据

### 2.1 JWT 密钥硬编码进仓库 —— `backend/src/main/resources/application.yml:77`
```yaml
jwt:
  secret: ec86ce46-f6f3-4075-8114-7dad9bc3b08e   # 写死在源码，随 git 提交
  expiration: 86400000
```
- 密钥随仓库提交，任何人拿到即可用 HS256 伪造任意用户/角色 token（含 `super_admin`）。
- 同一文件 `:7-8` 数据库用户名 `wanglei` 写死、密码为空 `password:`——生产若照此部署等于裸库。
- **修复**：密钥走 `${JWT_SECRET}` 环境变量 / Secret Manager，立即轮换；DB 密码从环境变量注入，禁止空密码与明文。
- 注：`docker-compose.yml` 里 `JWT_SECRET: ${JWT_SECRET}` 本就是从 env 读，但 **application.yml 里又写了一份硬编码值**，本地 `./mvnw` 启动直接用它，等于形同虚设。

### 2.2 XSS + localStorage token = 完整账户接管链（前端 · 已验证）
- **XSS 注入点**：`frontend/src/components/modals/ProductDemoModal.tsx:573`
  ```tsx
  <span dangerouslySetInnerHTML={{ __html: m.content
    .replace(/\*\*(.+?)\*\*/g, '<strong ...>$1</strong>')
    .replace(/\n/g, '<br/>') }} />
  ```
  `m.content` 是 AI（avatar 角色）生成的文本，仅做了加粗/换行替换，**任何 HTML 标签原样输出**。AI 内容可被提示注入（练习模式用户构造消息、或语料投毒）诱导输出 `<img src=x onerror=...>`、`<script>`。
- **放大器**：token 明文存 `localStorage`（`src/lib/storage.ts:10-13`、`src/lib/api/client.ts:46`）。任一 XSS 即可 `localStorage.getItem('token')` 盗走 JWT 并外发，含管理员账号完全被接管。
- **修复**：
  1. token 改用后端 `Set-Cookie: HttpOnly; Secure; SameSite=Strict` 下发，前端不再手动读写；
  2. 所有 AI 内容渲染走 `react-markdown`（默认不渲染原始 HTML）或 `DOMPurify.sanitize(html, {ALLOWED_TAGS:['strong','br'], ALLOWED_ATTR:['class']})`，**绝不用 `dangerouslySetInnerHTML` 拼 AI 文本**；
  3. 加 CSP `script-src 'self'` 兜底。

### 2.3 IM 回调零签名校验（后端 · 已验证）
- `service/ImGatewayService.java:77-134` `handleFeishuCallback` 解析消息并构造回复，但**完全不校验** `X-Feishu-Signature` / `timestamp` / `nonce`；且 `companyId` 在 `:87` 被硬编码为 `c0000000-...-0001`。
- `config/SecurityConfig.java:57` 的 `/im/*/callback` 是 `permitAll`。任何人均可 POST 触发业务（IDOR + 伪造）。
- **修复**：按平台算法用 `app_secret` 校验签名；`companyId` 从渠道配置/签名载荷解析，不可硬编码。

### 2.4 越权 / IDOR：资源按 ID 直查、无归属校验（后端）
- `ReportController.getReport/updateReport/downloadReport`、`InterviewController.getSession/getMessages/restart/pause`、`SkillController.getConversationMessages/deleteConversation`：这些接口只需 `authenticated()`，按主键直查、不校验「资源是否属于当前用户/企业」。
- 登录用户可枚举任意 report/session/conversation 的私密对话与报告内容。
- **修复**：所有按 ID 访问的接口在 Service 层补 ownership/companyId 校验（session→space→user→company 链路、report→space→company、conversation→user），不匹配抛 403/404。

---

## 3. 【CRITICAL/HIGH · 性能红线】事务里跑 AI/HTTP（与 CLAUDE.md 直接冲突）

> 以下全部违反 `CLAUDE.md`：「事务内禁止 AI 调用 / HTTP 调用 / 文件 IO」「事务持有 < 100ms」「连接池：并发×事务持有 ≤ 池×0.7」。

- **C1 素材清洗整条 AI 管线在 `@Transactional` 内**：`MaterialCleaningScheduler.tryCleanOne`（`:97`）+ `MaterialCleaningService.clean()`（`:576`）串行执行 10+ 次 AI/HTTP 调用（tagContext→extractInsights→classifyScenes→verifyGrains→mergeOverlapping→discoverPatterns→extractFaq→generateNarrative→generateProfile），且对每个颗粒再 `saveAndFlush` + `embeddingService.embed`（AI）。事务持库连接**数分钟**——随时拖垮整个应用（连接池耗尽 → 全站 500）。
- **C2 文件解析的 HTTP 调用落在事务内**：`MaterialCleaningScheduler.tryParseOne`（`:71`）+ `MaterialCleaningService.parseFile`（`:224-229` `webClient.post()...block()`）。外部 HTTP（可能超时几十秒）在 DB 事务内。
- **C3 萃取师分析 AI+HTTP 在 `@Transactional` 内**：`ExpertService.analyzeMaterials`（`:157`）+ `:181` webClient、`+245` chatClient、`+253` 再 AI，入口 `ExpertAnalysisScheduler.scanAndProcess`（`:82`）。
- **H8 SSE 流式在 HTTP 线程被 AI 长时间阻塞且无超时**（见第 1 节）。
- **H9 每轮对练在 HTTP 线程内多次同步 Embedding（AI）**：`PracticeAnalyzer.analyzeTurn`（`:35-58`）对 `expectedBehaviors` 每个元素同步 `embed`，一次用户消息触发 N 次向量化 + 多次 chatClient，全在请求线程。
- **H1 维护接口 `backfillEmbeddings`**：`findAll()` 全表 + 循环内同步 `embed` + `jdbcTemplate.update`，OOM / 线程卡死。
- **修复范式（项目里已有正确样板，照抄）**：`InterviewService` 的「DB 写入用自注入短事务（`self.persistUserMessage`/`markPhaseAndSaveTransition`）+ AI 流式在事务外」是对的；`ExpertAnalysisScheduler` 乐观锁抢占 + `REQUIRES_NEW` 锁事务也是对的。把 AI/向量化/HTTP 全部移出事务，用独立 `REQUIRES_NEW` 短事务做锁定与落库，向量化走 `@Async("embeddingExecutor")`。

---

## 4. 【HIGH · 正确性】"零 Mock" 原则被自己违反

`CLAUDE.md` 明文「零 Mock，零假数据：AI 调用失败抛异常，不造假」。但代码多处静默降级：

- **AI 服务 `report_generator.py:63-65`**：`except Exception: return {"content_json": self._fallback_report(messages)}`——LLM 超时/鉴权失败/网络错误**全部**降级为硬编码占位文案（如 `"请销冠在访谈中补充具体话术"`）。后端会把这**假报告当真报告写入 `report` 表**，污染业务数据且无人察觉。比报错更危险。
- **AI 服务 `report_generator.py:132-156`**：JSON 解析失败静默返回 `{}`，`json.dumps({})` 当 `content_json` 落库。无任何 schema 校验。
- **后端 `ReportController.downloadReport`**：文件未生成时返回 `report.content_json` 并冒充 `report_<id>.json` 下载（`M7`）；底层 `regenerateFile` 实际是空实现（返回 `""`），下载基本永远走 fallback。
- **AI 服务 `parse-file` 图片/音频分支**：在解析接口里塞 UI 提示文案（"请用 ChatGPT…"）给后端当正文，语义错乱且依赖调用方记得看 `needs_manual`（`MEDIUM-6`）。
- **`MaterialCleaningService.verifyGrains:398`**：批次异常时 `approved.addAll(batch)`——验证失败的低质颗粒直接放行，与"AI 失败不造假"精神冲突。
- **修复**：失败要响亮——记录日志、向上抛错或返回 `{"valid":false,"error":...}`，绝不能把占位当正文入库；报告下载未就绪返回 409/202 + 状态，前端轮询，而非返回 JSON 冒充。

---

## 5. 【HIGH · 查询/索引】N+1、全表扫、索引失效

- **H2 `SkillService.enterpriseChatToWriter:261`**：`grainRepository.findAll()` 后 Java 内存 filter+sort+limit(20)，随表增长线性恶化。
- **H3 `ReportService.toListResponse:224`**：分页每页 20 条，每条调 `resolveAuthorName`（再查 space+user）+ `findBySpaceId` 全量加载该空间**全部颗粒**只为取前 5 标签 → 约 20×(2+全量颗粒) 次查询。
- **H4 `ExpertService.toListResponse:657` / `loadCompositeExpertKnowledge:604`**：循环 `findByExpertId` / `findByExpertIdAndStatus` → N+1。
- **H5 `InterviewService.loadCompositeExpertKnowledge:604` / `resolveExpertSkillUsed:710`**：会话创建/响应时循环查颗粒。
- **H6 `ReportRepository.searchFullText:51`**：`LOWER(title) LIKE '%kw%' OR CAST(content_json AS string) LIKE '%kw%'`——前导通配符 LIKE 无法命中索引，对大 JSONB 全表扫。
- **H7 `GrainRetriever:47-58`**：`ORDER BY (1.0 - (g.embedding <=> ?)) * COALESCE(g.weight,1.0) DESC LIMIT ?`——pgvector 仅在**裸距离表达式** `ORDER BY embedding <=> ? LIMIT k` 时才走 HNSW 索引；加了相关性计算后规划器做全空间顺序扫描+排序，ANN 索引失效。
- **H12 `ToolController.getTools:45` / `SkillMaterialController.picker:59`**：`findAll()` 无 LIMIT 且跨租户可见。
- **H13 `V1__init.sql`**：`skill_material` / `expert_skill` 缺 `(status, locked_at)` 组合索引，调度器按 status+locked_at 全表扫。
- **修复**：分页游标 + 索引查询；批量 `findByXxxIn(ids)` 后内存分组（参考 `SkillService.listAllSkills` 已有的正确写法）；全文检索改 `tsvector`/GIN；RAG 改为先 `ORDER BY embedding <=> ? LIMIT k` 命中索引、相关性在 Java 端重排；补 status 组合索引。

---

## 6. 【HIGH · 前端】鉴权/流/错误处理

- **H1 无 `middleware.ts`，RBAC 仅为"隐藏导航"**：`AppLayout.tsx` 在 `useEffect` 里 `getToken()` 为空才跳登录，且**从不校验角色**；`/admin/*` 页面自身无角色校验。用户可直接在地址栏访问管理页（纵深防御缺失）。修复：加 `middleware.ts` 在 Edge 端校验会话+角色(配合第 2.2 的 HttpOnly cookie 才真正有效)。
- **H2 SSE 流在组件卸载时从不 abort**：`interview/[sessionId]/page.tsx`、`skill/[skillId]/page.tsx` 的 `abortRef` 赋值后**无 cleanup effect**，下次发送覆盖旧引用 → 旧流永不 abort，连接泄漏 + 卸载后 setState 警告。修复：加 `useEffect(()=>()=>abortRef.current?.abort(),[])`。
- **H3 `apiClient` 不处理 401**：`src/lib/api/client.ts:67` 收到 401 仅抛错不清登录态、不跳登录，用户卡死在失效态。修复：统一拦截 401 → `clearAuth()` + 跳登录。
- **M1 `connectSse` 声明了 `timeout` 却从未实现，且无重试**：网络抖动/代理超时后界面永久卡"AI 正在输入"。修复：实现超时 abort + 有限次退避重连 + 消费 heartbeat 重置计时。
- **M3 大量 `as any`**：访谈完成取 `reportId` 用 `(s as any).reportId`，为空时跳转 `/report/undefined/done`。修复：显式类型 + 判空。
- **M4 流式渲染每条 chunk 全量 `map` 消息数组（O(n²)）**：长会话随消息数线性放大。修复：流式文本用独立 `streamingText` 状态，完成再并入。
- **L1 默认凭据写死**：登录预填 companyId、管理后台新建用户默认密码 `'123456'`。

---

## 7. 【MEDIUM/LOW · 其余值得修的点】

- **C5 类 并发**：`AuthService.register:104` 先查后插 + 全局唯一 `account`（应为 `UNIQUE(company_id, account)`）→ TOCTOU 竞态 + 跨企业同账号被拒。`SkillService.submitFeedback:360` 读改写计数无乐观锁 → 丢失更新。
- **M1 类 分层**：`SkillProfileController` / `ToolController` 直接注入 Repository 写业务逻辑并返回 JPA 实体（违反 Controller 薄、DTO 分离，且易触发 Lazy 异常）。
- **M5 日志泄露 PII**：`ChatStreamAdapter:36-40/84-88` 把用户对话全文打 INFO；`application.yml:100` `com.aiextract: DEBUG` 生产应关。
- **M6 异常信息泄露给客户端**：`SkillController:127` / `ReportController:157` 自行吞异常返回 `e.getMessage()`，绕过 `GlobalExceptionHandler` 脱敏。
- **AI 服务 `expert_composer.py:65-100`**：`conflicts` 永远为空（宣称的 consensus/exclusive/contradiction 三策略缺矛盾）、`rule[:50]` 作分组键极脆弱、未按 `expert_name` 去重 → "共识"标注不可信。
- **AI 服务 `skill_loader.py` 热加载不全**：`on_modified` 只 `clear()` 文件缓存，核心 `BASE_TEMPLATE` 类属性改了不刷新（`reload_base_template` 定义了却从未被调用）。
- **AI 服务 `interview_engine.py:148` 阶段检测靠朴素子串**：「然后呢」「具体是」是中文日常高频词 → 误触发 SSE 阶段事件；建议改为 LLM 输出结构化标记。
- **AI 服务 `main.py` 用废弃 `@app.on_event`**：迁移到 `lifespan`；`requirements.txt` 依赖 `python-dotenv` 但从不 `load_dotenv()`，本地 `.env` 被忽略。
- **死代码/未用导入**：`routers/interview.py:1,10` 模块级 `import base64/httpx` 从未使用。

---

## 8. 优先级行动清单

**P0（24h 内，安全止血）**
1. 轮换并移除硬编码 JWT 密钥（`application.yml:77`）+ 补 DB 密码（2.1）
2. 消灭 `dangerouslySetInnerHTML` 渲染 AI 内容 + token 改 HttpOnly cookie（2.2）
3. IM 回调加签名校验、移除硬编码 companyId（2.3）
4. 所有按 ID 访问接口补归属校验（2.4）

**P1（本周，架构 + 红线）**
5. 决策 AI 服务去留：要么删除僵尸服务+清理文档，要么真正接回流式推理（1 / 4）
6. 把 LLM/向量化/HTTP 全部移出事务，迁到异步线程池 + `SseEmitter`/超时（3）
7. 去掉 `report_generator` / `downloadReport` 的静默假数据（4）
8. 修 N+1 / 全表扫 / HNSW 索引失效 / 补 status 索引（5）

**P2（下个迭代，健壮性 + 体验）**
9. 前端 `middleware.ts` RBAC + SSE abort + 401 处理 + 超时重连（6）
10. 并发写乐观锁 / 唯一约束修正（7）
11. 日志脱敏、异常脱敏、热加载补全、阶段检测结构化（其余 MEDIUM/LOW）

---

### 审查亮点（做得对的，保留）
- `InterviewService` 的「短事务自注入 + 事务外流式」模式、`ExpertAnalysisScheduler` 乐观锁抢占范式正确，值得在重构时作为模板推广。
- `GlobalExceptionHandler` 对 `BusinessException`/`DataIntegrityViolationException` 的脱敏处理正确（仅 2 处绕过需修）。
- `SkillService.listAllSkills` 用 `findAllById` + `countBySpaceIdIn` 批量查询避免 N+1，是良好范例，建议 `ReportService`/`ExpertService` 对齐。
