# 多领域实战方案：从真实领域到商业壁垒

> 核心原则：**始终保证产品效果**。多领域不是为了"能跑"，而是为了"每个领域都萃取得准、回答得好"。

---

## 一、从真实领域出发：我们到底要支持什么？

### 1.1 候选领域及其本质差异

不谈抽象的"多领域"，直接看5个真实领域的知识结构和素材形态：

| 维度 | 销售 | 金融 | 医疗 | 法律 | 教育培训 |
|------|------|------|------|------|---------|
| **专家是谁** | 销冠 | 研究员 | 资深医生 | 资深律师 | 骨干教师 |
| **经验形态** | 对话+行为 | 研究报告+判断 | 病例+诊疗决策 | 案件+法律推理 | 教学案例+方法 |
| **素材格式** | 聊天记录 | 研报/笔记 | 病历/录音转写 | 判决书/案卷 | 教案/课堂录播 |
| **知识颗粒** | 场景+话术+判断 | 框架+逻辑+结论 | 症状+诊断+方案 | 案情+推理+策略 | 学情+方法+效果 |
| **第一人称** | 多（"我当时说"） | 少（第三人称分析） | 少（病历被动语态） | 少（判决书第三人称） | 中（"我注意到学生"） |
| **对话结构** | 强（客户/销售轮替） | 弱（报告无对话） | 中（医患问答） | 弱（文书无对话） | 中（师生问答） |
| **质量容忍度** | 中（70%可用） | 中高（85%） | 极高（95%+） | 高（90%+） | 中（75%） |
| **错误代价** | 丢单 | 投资亏损 | 误诊危及生命 | 败诉/冤案 | 教学效果差 |

### 1.2 这些差异带来的架构挑战

从高级架构师视角，每个差异都对应一个设计决策：

**挑战1：素材格式多样性**
- 销售素材是聊天记录——有"客户/销售"角色标记，有对话轮次
- 医疗病历是结构化文档——"主诉/现病史/查体/诊断/治疗方案"
- 法律判决书是半结构化文书——"原告称/被告辩称/本院认为/判决如下"
- **当前系统的StructureAnalyzer硬编码了"客户/顾客/买家/销售/销冠"角色词，对非对话素材直接失效**

**挑战2：知识颗粒结构异构**
- 销售颗粒：场景描述+销冠内心OS+标准话术+常见失误+适用条件
- 医疗颗粒：病例场景+诊断思维+诊疗方案+常见误诊+适应症禁忌
- 法律颗粒：案件情境+法律推理+诉讼策略+常见败因+适用情形
- **形式上5字段可以复用，但`standard_script`（标准话术）这个字段名在医疗场景语义严重偏移——AI会把"诊疗方案"填成"医生对患者说的话"**

**挑战3：质量门槛差异**
- 销售可以容忍"大概对"——一个锦囊70分就可用
- 医疗必须"精确对"——一条诊疗策略如果错了可能危及生命
- **当前系统对所有领域用同一套质量检查参数，无法按领域调精度**

**挑战4：冷启动质量**
- 新领域第一天没有颗粒，分身只能回答"暂无经验数据"
- 如果用通用知识充种子，种子质量低会稀释整体效果
- **必须有领域种子机制，但种子必须是经过验证的结构化框架，不是随便抓的网页内容**

**挑战5：效果度量**
- "效果好不好"不能靠感觉——必须可量化
- 销售效果：对练评分提升率
- 医疗效果：诊断思路与专家一致率
- **当前系统只有一次性的对练评价，没有持续的效果追踪**

---

## 二、高级架构师设计：方案选型

### 2.1 三种架构方案

#### 方案A：纯配置化（只加YAML）

**做法**：不改Java代码，每个新领域只写YAML配置 + Prompt文件

**代码审计结论：不可取**。经逐行代码实查，当前系统有6处硬编码的销售领域假设：

| # | 位置 | 问题 | 后果 |
|---|------|------|------|
| B1 | ContentAnalyzer.java:39 | 拒绝码`NOT_SALES_DOMAIN` + "本平台专注于销售经验萃取"文案硬编码 | 医疗论文/法律文档被拒绝时提示语荒谬 |
| B2 | ContentAnalyzer.java:46 | 教科书检测要求第一人称占比≥0.01 | 论文/判决书第三人称被动语态会被误杀 |
| B3 | MaterialCleaningService.java:1036 | ContextTags record硬编码`buying_stage/buyer_persona`等5个销售字段 | 金融领域已坏（全null），医疗/法律同样全null |
| Q2 | StructureAnalyzer.java:44 | countRoleAlternations硬编码"客户/顾客/买家/销售/销冠" | 医患对话、法律对话检测不到对话轮 |
| Q5 | TextNormalizer.java:19 | "超标"→"招标"硬编码纠错 | 医疗文本"指标超标"被误改 |
| Q8 | ChatStreamService.java:237 | enterpriseChat硬编码`"sales.b2b_enterprise"` | 跨域企业问答用销售prompt |

**纯配置化无法解决这些问题**——YAML只能控制阈值和关键词，不能改变Java代码的硬编码逻辑。医疗论文会被Gate1拒绝，情境标注会全null，结构预检会打矛盾分。

#### 方案B：配置化 + 领域适配层（推荐）

**做法**：修复6处硬编码 + 保留YAML配置体系 + 新增领域适配抽象

这是"修3个P0 + 2个P1"就能让系统真正支持多领域的方案。改动量小，但每处改动都直接解决效果问题：

**P0修复（必须，影响阻断/效果）**：

```
P0-1: ContextTags → Map<String, String>
  改动: MaterialCleaningService.java
  效果: 情境标注从YAML的context_dimensions动态读取字段，不再硬编码5个销售字段
  代码量: ~50行改动

P0-2: Gate1拒绝码/文案配置化
  改动: ContentAnalyzer.java + DomainConfig.AcceptanceConfig
  效果: 拒绝文案从YAML的reject_message读取，不再说"请上传销售素材"
  代码量: ~20行改动

P0-3: StructureAnalyzer角色词配置化
  改动: StructureAnalyzer.java + DomainConfig.PreCheckConfig
  效果: 对话角色词从YAML的role_labels读取，支持"医生/患者""律师/当事人"
  代码量: ~30行改动
```

**P1修复（重要，影响质量）**：

```
P1-1: standard_script字段语义重构
  改动: ExperienceGrain.java + extraction prompt
  效果: 字段名改为actionable_knowledge，label从YAML读取
  代码量: 数据库migration + ~20行改动

P1-2: FAQ提取按域开关
  改动: MaterialCleaningService.java + DomainConfig.PipelineConfig
  效果: 非对话领域可关闭L9 FAQ提取
  代码量: ~10行改动

P1-3: 企业问答domain从Skill读取
  改动: ChatStreamService.java:237
  效果: 不再硬编码sales.b2b_enterprise
  代码量: ~5行改动
```

**核心代码改动示例——P0-1 ContextTags重构**：

```java
// 改前: 硬编码5个销售字段
record ContextTags(String buyingStage, String buyerPersona,
    String competitiveContext, String dealSizeHint, String industrySignals) {
    String toPromptPrefix() {
        return String.format("购买阶段：%s | 买方角色：%s | ...", ...);
    }
}

// 改后: 从YAML配置动态构建
record ContextTags(Map<String, String> tags) {
    String toPromptPrefix() {
        if (tags == null || tags.isEmpty()) return "";
        StringBuilder sb = new StringBuilder("## 对话情境\n");
        tags.forEach((k, v) -> sb.append(k).append("：").append(v != null ? v : "未知").append("\n"));
        return sb.toString();
    }
}

// tagContext() 改为从YAML的context_dimensions动态读取
private ContextTags tagContext(String text, String domain) {
    DomainConfig config = domainConfigLoader.load(domain);
    List<ContextDimension> dimensions = config.getPipeline().getContextDimensions();
    // prompt中要求AI按dimensions的name输出JSON
    String prompt = buildContextTagPrompt(text, dimensions);
    Map<String, Object> aiResult = callAI(prompt);
    Map<String, String> tags = new LinkedHashMap<>();
    for (ContextDimension dim : dimensions) {
        tags.put(dim.getLabel(), (String) aiResult.get(dim.getName()));
    }
    return new ContextTags(tags);
}
```

**YAML配置示例——医疗领域**：

```yaml
# domain/healthcare.yml
extends: base
domain:
  id: healthcare
  name: 医疗健康
  role_label: 医生
  counterparty_label: 患者
  skill_label: 临床决策能力
  knowledge_unit: 诊疗策略

acceptance:
  min_text_length: 150
  reject_message: "未检测到足够的医疗相关内容，请上传包含诊疗过程的病历、病例讨论或临床经验素材"
  first_person_ratio_min: 0.0  # 关闭第一人称检查（病历是第三人称）
  theoretical_ratio_max: 0.50   # 放宽（学术论文"应/需要"较多）
  sales_keywords: [诊断, 治疗, 手术, 用药, 症状, 体征, 影像, 病理, 随访, 预后]

precheck:
  min_dialogue_turns: 0  # 非对话素材不做轮次检查
  min_customer_ratio: 0
  role_labels:           # 新增: 对话角色词配置
    expert: [医生, 我, 主治, 主任]
    counterparty: [患者, 病人, 家属]
  scene_mapping:
    - label: 急诊决策
      keywords: [急诊, 危重, 抢救, 生命体征, 分诊]
    - label: 门诊诊疗
      keywords: [门诊, 主诉, 查体, 辅助检查, 处方]
    - label: 手术决策
      keywords: [手术, 术前, 术后, 并发症, 麻醉]
    - label: 疑难病例
      keywords: [疑难, 会诊, 多学科, 鉴别诊断, 罕见]

pipeline:
  context_dimensions:      # 新增: 情境维度从YAML读取
    - name: disease_type
      label: 疾病类型
      type: free_text
    - name: treatment_stage
      label: 治疗阶段
      type: enum
      values: [初诊, 复诊, 术前, 术中, 术后, 随访]
    - name: severity
      label: 病情严重度
      type: enum
      values: [轻, 中, 重, 危重]
  extraction_role: 临床决策模式分析专家
  extraction_task: 发现医生的诊断思维、决策依据和治疗方案选择逻辑
  extraction_output_fields:
    - name: scene_description
      label: 病例场景
    - name: expert_thought
      label: 诊断思维
    - name: actionable_knowledge    # 改名: standard_script → actionable_knowledge
      label: 诊疗方案
    - name: common_mistakes
      label: 常见误诊
    - name: applicable_condition
      label: 适应症与禁忌
  faq_enabled: false  # 新增: 非对话领域关闭FAQ提取

chat:
  modes: [qa, talk, practice]
  practice_intents:
    - label: 急诊决策
    - label: 门诊诊疗
    - label: 手术决策
    - label: 疑难病例
```

#### 方案C：领域插件系统

**做法**：每个领域是一个独立插件，有自己的管道、质检规则、评估指标

**不推荐作为当前阶段方案**，原因：
- 过度工程化——当前只有2个领域，插件系统要定义SPI接口、生命周期、依赖管理，投入产出比极低
- 维护成本高——N个插件意味着N套代码要维护，bug fix和feature要在N个地方做
- 但作为**未来演进方向**是合理的——当领域超过15个、且某些领域有完全不同的管道需求时

### 2.2 方案B的完整架构

```
┌──────────────────────────────────────────────────────────────┐
│                      统一服务层 (不改)                        │
│  ChatStreamService / GrainRetriever / 对抗验证L6 / RAG检索   │
│  → 这一层是完全领域无关的，无需任何改动                       │
├──────────────────────────────────────────────────────────────┤
│                    领域适配层 (新增/修复)                      │
│  ContextTagAdapter    → 从YAML动态读取情境维度               │
│  AcceptanceAdapter    → 拒绝码/文案从YAML读取                │
│  StructureAdapter     → 角色词从YAML读取                     │
│  GrainFieldAdapter    → 颗粒字段label从YAML读取              │
│  QualityGateAdapter   → 质量阈值按领域配置                   │
├──────────────────────────────────────────────────────────────┤
│                    领域配置层 (已有+增强)                      │
│  DomainConfig YAML    → 5段配置 + 新增role_labels等字段       │
│  DomainConfigLoader   → mtime热加载 (详见配置化方案)          │
│  PromptLoader         → 已有mtime热加载                       │
├──────────────────────────────────────────────────────────────┤
│                    效果保障层 (新增)                           │
│  QualityMetricsCollector → 按领域采集萃取质量指标             │
│  EffectTracker           → 对练/问答效果持续追踪             │
│  DomainHealthMonitor     → 领域健康度实时监控                │
└──────────────────────────────────────────────────────────────┘
```

---

## 三、效果保障机制：如何始终保证产品效果

### 3.1 效果度量体系

"效果好不好"不能靠感觉。建立三层度量：

#### 第一层：萃取质量（事前）

```sql
CREATE TABLE extraction_quality_metrics (
    domain_id       VARCHAR(64),
    report_id       UUID,
    total_grains    INT,              -- 萃取颗粒总数
    avg_quality_score FLOAT,          -- L6对抗验证平均分(0-10)
    pass_rate       FLOAT,            -- 质量分≥7的颗粒占比
    null_scene_ratio FLOAT,           -- 场景为空的颗粒占比(应<10%)
    null_context_ratio FLOAT,         -- 情境为null的占比(应<5%)
    duplicate_ratio FLOAT,            -- 重复颗粒占比(应<15%)
    extraction_time_ms BIGINT,        -- 萃取耗时
    created_at      TIMESTAMP DEFAULT NOW()
);
```

**效果红线**：
- `avg_quality_score` ≥ 7.0 → 萃取质量达标
- `pass_rate` ≥ 70% → 大部分颗粒可用
- `null_context_ratio` < 5% → 情境标注有效
- 任一红线不达标 → 领域配置需要调优

#### 第二层：使用效果（事中）

```sql
CREATE TABLE usage_effect_metrics (
    domain_id       VARCHAR(64),
    metric_date     DATE,
    chat_sessions   INT,
    helpful_count   INT,              -- 用户标记"有帮助"次数
    unhelpful_count INT,              -- 用户标记"没帮助"次数
    satisfaction_rate FLOAT,          -- helpful/(helpful+unhelpful)
    practice_sessions INT,
    practice_avg_score FLOAT,         -- 对练平均分
    practice_improvement FLOAT,      -- 前后对练评分提升率
    seed_ratio      FLOAT,           -- 种子颗粒占比(应持续下降)
    PRIMARY KEY (domain_id, metric_date)
);
```

**效果红线**：
- `satisfaction_rate` ≥ 75% → 用户认可回答质量
- `practice_improvement` ≥ 15% → 对练有学习效果
- `seed_ratio` 周环比下降 → 真实萃取在替代种子

#### 第三层：领域健康度（事后）

```
领域健康度 = avg_quality_score × 0.25
           + satisfaction_rate × 0.30
           + practice_improvement × 0.20
           + (1 - seed_ratio) × 0.15
           + (1 - null_context_ratio) × 0.10

健康度 ≥ 0.75 → 领域运行良好
健康度 0.50-0.75 → 需要关注，调优配置
健康度 < 0.50 → 领域需要干预（可能配置有问题或种子质量差）
```

### 3.2 质量保障流程

```
新领域上线流程:

Step 1: 配置校验
  → DomainConfigValidator 校验YAML完整性
  → 必填字段检查、关键词库≥3组、场景映射≥3类

Step 2: 样本测试 (关键!)
  → 用3组真实素材走完整管道
  → 检查: 准入是否通过？情境标注是否有效(非全null)？
  → 检查: 萃取颗粒质量分≥7？场景标签合理？
  → 如果不通过 → 调整配置 → 重测

Step 3: 冷启动验证
  → 种子框架加载后，用10个典型问题测试Chat
  → 检查: 回答是否相关？是否标注了"种子知识"？
  → 检查: 种子颗粒是否有明显错误？

Step 4: 效果监控
  → 上线后每天采集效果指标
  → 健康度看板实时展示
  → 任一红线连续3天不达标 → 自动告警
```

### 3.3 质量回归防护

每次修改领域配置后，自动跑回归测试：

```java
@Service
public class DomainQualityRegression {
    // 每次 domain config 变更后触发
    public RegressionResult runRegression(String domainId) {
        // 1. 加载该领域的标准测试素材集（3-5组）
        List<TestMaterial> testSet = loadTestSet(domainId);

        // 2. 逐组跑完整管道
        for (TestMaterial tm : testSet) {
            ExtractionResult result = materialCleaningService.clean(tm.getText(), domainId);

            // 3. 检查质量指标
            checkAcceptance(result);        // 准入是否通过
            checkContextTags(result);       // 情境是否非null
            checkGrainQuality(result);      // 颗粒质量分≥7
            checkSceneTags(result);         // 场景标签是否合理
        }

        // 4. 与上次结果对比
        return compareWithBaseline(domainId);
    }
}
```

**核心原则：配置改了不是直接生效就完了，而是先跑回归测试，确认效果没退化才允许发布。**

---

## 四、商业壁垒分析

### 4.1 方案B如何构建壁垒

多领域本身不是壁垒——竞品也能加YAML配置。真正的壁垒在于**效果飞轮**：

```
更多领域 → 更多素材 → 更多萃取数据 → 更好的元萃取(方法论)
    ↑                                          ↓
    └── 跨域方法论迁移增强新领域冷启动 ←─── 方法论池积累
                    ↓
              新领域冷启动质量更高
                    ↓
              用户更快看到效果 → 更愿意上传素材
                    ↓
              更多素材 → 飞轮加速
```

**壁垒的三个层次**：

#### 层次1：数据壁垒（最浅但最先发优势）

- 每个领域的真实专家素材是稀缺资源——销冠的实战对话、名医的诊疗经验
- 先进入的领域积累的数据越多，后进入者越难追
- **关键动作**：尽快在2-3个标杆领域积累足够多的真实素材（每域≥500条高质量颗粒）

#### 层次2：方法壁垒（核心差异化）

- 11层清洗管道 + 对抗验证(L6) + 元萃取——这套方法论是经过实战打磨的
- 竞品即使抄了架构，也要花6-12个月调优每个层的参数
- **关键动作**：把元萃取的成果（萃取方法论本身）作为产品能力对外输出——"我们的AI不仅萃取知识，还在持续优化萃取方法"

#### 层次3：领域知识壁垒（最深但最重）

- 每个领域的种子框架、关键词库、场景分类体系——这些是领域know-how
- 不是技术能复制的，需要行业专家参与构建
- **关键动作**：与行业专家合作构建领域种子框架，形成"平台+行业专家"的生态

### 4.2 方案A/C的壁垒问题

| 方案 | 壁垒问题 |
|------|---------|
| 方案A（纯配置化） | 效果差→用户不信任→不传素材→无数据壁垒→无飞轮。**没有效果就没有壁垒** |
| 方案C（插件系统） | 壁垒最强但太重——2周/域的投入在当前阶段无法形成规模，等做到5个领域时竞品已经用方案B做到10个了 |

### 4.3 商业化模型设计

```
三层定价:

基础层 (免费/低价)
  → 2个标杆领域(销售+金融)
  → 每域500条颗粒上限
  → 基础Chat+对练
  → 目标: 获客、验证效果

专业层 (按域订阅 ¥X,000/月/域)
  → 行业领域(医疗/法律/教育/HR)
  → 不限颗粒数
  → 冷启动种子包
  → 效果看板
  → 目标: 行业深耕

企业层 (定制 ¥X0,000+/年)
  → 定制领域配置
  → 跨域方法论迁移
  → 私有化部署
  → 效果SLA保障
  → 目标: 大客户锁定
```

**关键商业指标**：
- 领域健康度 → 直接影响续费率
- seed_ratio → 衡量用户投入度（种子占比越低说明用户传了越多真实素材）
- practice_improvement → 直接证明产品价值（对练提升了X%）

### 4.4 不合理的地方（诚实评估）

1. **医疗/法律领域的合规风险**——医疗建议需要 disclaimer，法律建议需要执业律师审核。纯粹AI萃取的"经验"不能直接作为医疗/法律建议。产品需要明确的边界声明和使用条款。

2. **领域专家获取成本高**——构建医疗种子框架需要找资深医生参与，这比写YAML配置贵得多。如果领域种子质量差，冷启动效果就差，用户第一天就流失。

3. **跨域迁移可能产生幻觉**——销售"异议处理"方法论迁移到医疗"患者顾虑应对"，如果迁移映射不准，分身可能用销售话术回答医疗问题，效果适得其反。

4. **效果度量本身有偏差**——用户标"有帮助"不代表真的有帮助。需要更客观的效果度量（如对练前后技能测试）。

5. **领域越多维护越重**——即使方案B维护成本低，10个领域也意味着10套YAML+10套Prompt要维护。如果没有效果回归测试，改一处可能影响多个领域。

---

## 五、实施路线图

### Phase 0: P0修复（1周）

| 任务 | 改动文件 | 效果保障 |
|------|---------|---------|
| ContextTags → Map | MaterialCleaningService.java | 情境标注不再全null |
| Gate1拒绝码配置化 | ContentAnalyzer.java | 新域不被荒谬拒绝 |
| StructureAnalyzer角色词配置化 | StructureAnalyzer.java | 非对话素材不矛盾打分 |
| TextNormalizer硬编码移除 | TextNormalizer.java | 医疗文本不被误改 |
| enterpriseChat domain修复 | ChatStreamService.java | 跨域问答正确路由 |

### Phase 1: 领域适配层（2周）

| 任务 | 说明 |
|------|------|
| GrainFieldAdapter | 颗粒字段label从YAML读取，standard_script→actionable_knowledge |
| FAQ按域开关 | pipeline.faq_enabled控制L9执行 |
| QualityGateAdapter | 质量阈值按领域配置（医疗strict/销售normal） |
| DomainConfigValidator | YAML完整性校验 |
| extraction_quality_metrics表 | 萃取质量采集 |

### Phase 2: 冷启动与效果保障（2周）

| 任务 | 说明 |
|------|------|
| 种子框架机制 | YAML声明seed_framework，结构化框架颗粒 |
| 样本测试集 | 每域3-5组标准测试素材 |
| DomainQualityRegression | 配置变更后自动回归测试 |
| usage_effect_metrics表 | 使用效果采集 |
| 领域健康度看板 | 前端效果可视化 |

### Phase 3: 第三个领域落地（2周）

| 任务 | 说明 |
|------|------|
| 医疗领域YAML+Prompt | 从base继承，覆盖差异化部分 |
| 医疗种子框架 | 与行业专家合作构建50-100条种子 |
| 医疗样本测试集 | 3组真实病历素材 |
| 上线验证 | 健康度≥0.75才允许发布 |

### Phase 4: 规模化（持续）

- 跨域方法论迁移机制
- 领域效果回归测试自动化
- 行业伙伴接入流程
- 领域商店商业化

---

## 六、核心设计决策

### 决策1: 颗粒字段是否需要数据库迁移？

**决策：需要，但分两步走。**

第一步（Phase 1）：不改数据库列名，只在Prompt和前端展示层用YAML的label覆盖。`standard_script`列名保持不变，但医疗领域展示为"诊疗方案"。

第二步（Phase 4，领域稳定后）：做数据库migration，`standard_script` → `actionable_knowledge`，一次性改干净。

理由：第一步让新域能用且语义正确，第二步在风险可控时做彻底重构。

### 决策2: 种子颗粒从哪里来？

**决策：行业专家构建，不是LLM自动生成。**

种子颗粒是领域冷启动的基础，如果种子错了，整个领域的效果都会被拖累。LLM自动生成的"种子"质量不可控——它可能生成听起来合理但实际错误的诊疗建议。

正确做法：
1. 与行业专家合作，人工构建50-100条结构化种子颗粒
2. 种子颗粒标注 `grain_type=seed, confidence=0.5`
3. Chat回答时标注"此建议来自行业通用框架，尚未经实战专家验证"
4. 真实萃取颗粒积累后，种子自动降权

### 决策3: 跨域迁移是否自动启用？

**决策：默认关闭，企业版可开启。**

跨域迁移有风险——销售方法论可能不适合医疗场景。默认关闭保证每个领域的效果不受其他领域干扰。企业版客户在理解风险后可主动开启，且迁移映射需要人工配置确认。

### 决策4: 先做哪个第三领域？

**决策：教育培训 或 客服，不做医疗/法律。**

理由：
- 医疗/法律合规风险高，效果容错率低，不适合作为第三个验证领域
- 教育培训的素材格式（教学案例）和当前系统最接近，冷启动门槛低
- 客服与销售的素材格式高度相似（都是对话记录），适配改动最小
- 用低风险领域验证方案B的有效性，再做高价值领域（医疗/法律）

---

## 七、总结

**一句话方案**：修3个P0硬编码 + 加领域适配层 + 建效果度量体系 → 方案B是效果、速度、壁垒的最佳平衡点。

**效果保证的核心**：
1. 事前——配置校验 + 样本测试，不通过不上线
2. 事中——萃取质量指标实时采集，红线告警
3. 事后——领域健康度看板，效果退化自动检测

**壁垒的核心**：不是配置本身（可复制），而是**效果飞轮**——更多领域→更多素材→更好萃取→更好效果→更多用户→更多素材。方案B通过保证每个领域的效果，启动这个飞轮。方案A因为效果差无法启动飞轮，方案C因为太重无法快速启动飞轮。

**最终判断**：方案B是"保证效果"约束下的最优解。P0修复只需1周，第三个领域2周可上线，且每个领域都有可量化的效果度量。这不是最完美的架构，但是当前阶段效果/速度/壁垒三角的最优平衡。
